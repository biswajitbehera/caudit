<!DOCTYPE html>
<html>
  <head>
    <% include partials/template/head.ejs %>
  </head>

<body id="<%= pageId %>">
  <!-- header and banner -->
      <% include partials/template/header.ejs %>
  <!-- header ends -->

<!-- Container -->
  <div class="container">
      <div class="row">
        <div class="col-sm-8">
          <div class="maincontent">
            <h2>Cloud Security Controls(CSCs)</h2>
            <h4>We have <%= controls.length %> CSCs are categorised into 4 key types. CSCs are generic and may used for assessing any public cloud deployment.</h4>

            <div class="table-responsive">
            <table class="table table-condensed">
              <thead>
                <tr>
                  <th>ID</th>
                  <th>Control</th>
                </tr>
              </thead>
              <tbody>
            <% if (controls.length>0){ var i=0;%>
            <%  controls.forEach(function(item){ %>
                <tr>
                  <td><%= item.id %></td>
                  <td><a href="controls/<%=i%>"><%= item.title %></a></td>
                </tr>
              <% i++;}) %>
            <% } %>
          </tbody>
        </table>
      </div>
          </div>
        </div><!-- primary -->
        <aside class="col-sm-4">

        </aside><!-- secondary -->
      </div>
    </div>
<!-- Container ends -->

<% include partials/template/footer.ejs %>

<script src="/reload/reload.js"></script>
</body>
</html>
